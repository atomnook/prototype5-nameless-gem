syntax = "proto3";

package protobuf.routine;

import "routine/expression.proto";

enum Condition {
  RAND = 0;
}

message Alternative {
  Expression id = 1;
  Condition condition = 2;
  Expression then = 3;
  Expression else = 4;
}
